fun main() {
    while (true) {
        println("===[숫자야구 게임]==========")

        // 게임 객체 생성 -> 생성과 동시에 정답이 정해짐
        var game: BaseballGame = BaseballGame()

        // 플레이어 객체 생성
        var player: BaseballPlayer = BaseballPlayer()


        break   // 임시
    }
}