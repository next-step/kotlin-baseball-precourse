//import org.junit.jupiter.api.Test
//import org.junit.jupiter.api.Assertions.assertEquals

/*
class AddTest {
    @Test
    fun `test generateSecretNumber`() {
        // Given
        val expectedLength = 3

        // When
        val secretNumber = generateSecretNumber()

        // Then
        assertEquals(expectedLength, secretNumber.size)
        secretNumber.forEach {
            assert(it in 1..9) { "Generated secret number should be between 1 and 9" }
        }
    }

    @Test
    fun `test checkGuess`() {

        val secretNumber = intArrayOf(1, 2, 3)

        var guess = intArrayOf(1, 2, 3)
        var result = checkGuess(guess, secretNumber)
        assertEquals(Pair(3, 0), result)

        guess = intArrayOf(1, 4, 5)
        result = checkGuess(guess, secretNumber)
        assertEquals(Pair(1, 0), result)
    }
} */